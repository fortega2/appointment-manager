# Credentials for the Postgres container in docker-compose.dev.yml.
# Copy to docker/.env and change the password; the file is gitignored.
POSTGRES_DB=appointment-manager
POSTGRES_USER=appointment-manager
POSTGRES_PASSWORD=change-me
