pre-commit:
  parallel: true
  commands:
    linter:
      run: golangci-lint run
    security:
      run: GOVERSION=$(go env GOVERSION | cut -d- -f1) govulncheck -show verbose ./...
    tests:
      run: go test -short ./...
    css-drift:
      glob: "**/*.templ"
      run: make check-css