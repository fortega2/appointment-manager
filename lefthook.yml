pre-commit:
  parallel: true
  commands:
    linter:
      run: golangci-lint run
    security:
      run: govulncheck ./...
    tests:
      run: go test -short ./...
    css-drift:
      glob: "**/*.templ"
      run: make check-css