package components

import "appointment-manager/internal/i18n"

// ModalCloseButton is the dismiss control shared by the entity modals. It drives
// the modalOpen flag of the Alpine scope its caller declares, so it only renders
// correctly inside one.
templ ModalCloseButton() {
	<button
		@click="modalOpen = false"
		type="button"
		aria-label={ i18n.T(ctx, "common.close") }
		class="absolute top-4 right-4 text-gray-400 hover:text-gray-600"
	>
		<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
	</button>
}

// FormActions is the cancel/submit footer shared by the entity forms. Like
// ModalCloseButton it expects an enclosing Alpine scope with a modalOpen flag.
templ FormActions() {
	<div class="mt-6 flex justify-end space-x-3">
		<button
			type="button"
			@click="modalOpen = false"
			class="bg-white py-2 px-4 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500"
		>
			{ i18n.T(ctx, "common.cancel") }
		</button>
		<button
			type="submit"
			class="inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500"
		>
			{ i18n.T(ctx, "common.save") }
		</button>
	</div>
}
